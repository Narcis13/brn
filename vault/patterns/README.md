# Patterns

Store reusable implementation, testing, and documentation patterns here once they have survived review and verification in a milestone.

`M001` seeds the directory but does not promote any patterns yet.

`M008` adds recommendation-only pattern candidates under `.supercodex/learning/patterns/`; promotion into this directory remains a separate human-reviewed step.
