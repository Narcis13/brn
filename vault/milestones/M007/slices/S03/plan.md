# S03 Plan

- Enqueue reviewed workers with canonical commits and declared regression commands.
- Add `integrate show` and `integrate run` for explicit serialized convergence.
- Block integration on stale base commits, missing completion artifacts, or regression failures.
