# S01 Boundary Map

- Queue eligibility depends on modern task contracts, dependency completion, likely-file ownership, and regression declarations.
- Worker records own one task unit, one task branch, one worktree, and one resource set.
- Queue item status is allowed to transition through `active`, `blocked`, and `ready_to_integrate`.
