# S02 Plan

- Extend runtime adapter dispatch and resume APIs with execution options.
- Add milestone and task branch/worktree helpers in the git layer.
- Make verification and completion paths workspace-aware with control-root fallback for task contracts.
