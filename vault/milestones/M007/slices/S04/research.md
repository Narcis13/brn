# S04 Research

- Parallel state is only trustworthy if doctor can detect orphaned worktrees and queue/lock mismatches.
- Metrics need dedicated counters for dispatch volume, successful convergence, and blocked integration attempts.
