# S04 Plan

- Implement `supercodex init`, `doctor`, `state`, `queue`, and `lock` commands.
- Keep git handling read-only in Phase 1.
- Back the doctor command with schema, placeholder, and consistency checks.
