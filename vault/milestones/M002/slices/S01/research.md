# S01 Research

- Keep runtime-specific argv details behind adapter modules.
- Use a shared dispatch packet shape that does not require Phase 3 synthesis.
- Persist probe snapshots to the registry so availability is inspectable from disk.
