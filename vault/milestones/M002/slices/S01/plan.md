# S01 Plan

- Define runtime ids, capabilities, dispatch packet, probe result, run handle, and normalized result types.
- Add runtime schemas and write them into `.supercodex/schemas/`.
- Implement runtime registry load, save, supports, and probe flows.
