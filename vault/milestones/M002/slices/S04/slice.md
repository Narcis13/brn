# S04: Runtime CLI And Manual Dispatch

Demo sentence: An operator can list runtimes, probe capabilities, dispatch a packet, and collect the normalized result from the CLI without Phase 3 automation.
