# S04 Review

Review target:

- Operators can exercise the adapter layer without touching internal modules.
- CLI errors stay actionable when packets, registry entries, or runtime output are invalid.
