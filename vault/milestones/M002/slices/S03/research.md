# S03 Research

- Prefer `claude -p` for non-interactive execution.
- Use explicit session ids so resume support does not depend on parsing opaque stdout.
- Handle wrapper JSON output defensively when extracting the structured payload.
