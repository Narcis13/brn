# S02 Plan

- Implement Codex probe, dispatch, normalize, collect, and resume handling.
- Persist temp run handles and raw output refs under `.supercodex/temp/runtime/`.
- Cover argv construction and normalization with stubbed tests.
