# S02 Research

- Prefer `codex exec` for non-interactive execution.
- Use structured output and raw output capture so normalization stays deterministic.
- Resume is best-effort and depends on runtime-native session identifiers.
