# S01 Plan

- Add recovery checkpoint and continuation schemas.
- Write structured continuation artifacts during next-action dispatch and result collection.
- Preserve backward compatibility with `continue.md` and `current.json.recovery_ref`.
