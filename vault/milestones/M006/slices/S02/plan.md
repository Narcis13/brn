# S02 Plan

- Add recovery assessment and persisted reconciliation reports.
- Wire `recover show` and `recover reconcile` into the CLI.
- Update next-action routing to respect recovery recommendations instead of blindly resuming interrupted runs.
