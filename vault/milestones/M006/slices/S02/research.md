# S02 Research

- Replace the old interrupted-run resume heuristic with a deterministic recovery assessment.
- Keep `runtime resume` intact and route orchestration through next-action plus recovery commands.
