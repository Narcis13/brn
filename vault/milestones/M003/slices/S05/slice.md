# S05: Headless Next-Action CLI

Demo sentence: A single CLI command can synthesize the next action, dispatch it through the chosen runtime, and persist the resulting run artifacts and state updates.
