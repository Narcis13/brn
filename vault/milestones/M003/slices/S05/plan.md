# S05 Plan

- Add `next-action` CLI commands for dry-run inspection and single-step dispatch.
- Wire together state reconcile, selection, context assembly, packet persistence, runtime dispatch, result collection, and state updates.
- Return structured JSON that can drive operator workflows now and a future autonomous loop later.
- Stop short of full daemonized orchestration; Phase 3 should prove one deterministic step end to end first.
