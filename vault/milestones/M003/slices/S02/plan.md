# S02 Plan

- Implement context-layer assembly that honors the `budget`, `balanced`, and `quality` profiles from `current.json`.
- Gather the current unit, milestone and slice docs, decisions, assumptions, feedback, and latest retry context into an explicit manifest.
- Build runtime-neutral dispatch packets automatically from synthesized context and unit metadata.
- Persist the generated packet and context manifest before any runtime execution starts.
