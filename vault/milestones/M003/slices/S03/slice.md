# S03: Canonical Run Records

Demo sentence: Every synthesized attempt creates a durable `.supercodex/runs/<run-id>/` record that links packet, prompt, git snapshot, runtime handle, normalized result, and continuation metadata.
