# S01 Research

- Keep the task-file contract human-readable, but strict enough that deterministic code can reject malformed artifacts.
- Do not retroactively force M001-M003 slices into the modern planning model.
- Prefer full unit IDs in task dependencies so queue ordering remains explicit.
