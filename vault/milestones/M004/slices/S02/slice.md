# S02: Planning-Aware Conductor Integration

Demo sentence: The conductor can route planning units to planner roles, persist the run, and return to `plan` only when the resulting artifacts validate from disk.
