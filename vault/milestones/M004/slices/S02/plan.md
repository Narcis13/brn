# S02 Plan

- Add `plan sync` and `plan validate` CLI commands.
- Route modern slices without valid tasks to `slice-planner`.
- After successful planning runs, synchronize the queue and transition back to `plan` only if validation passes.
