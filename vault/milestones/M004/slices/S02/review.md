# S02 Review

- Do not let planning success fall through to the implementation transition path.
- Keep runtime-neutral packets for planning units; runtime adapters should stay unchanged.
- Ensure invalid planning output cannot silently unblock downstream task execution.
